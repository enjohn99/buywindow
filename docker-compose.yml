services:
  buywindow:
    build:
      context: .
      dockerfile: Dockerfile
    image: buywindow:local
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 8080
    ports:
      - "8080:8080"
